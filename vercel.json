{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm turbo build --filter=@app/web",
  "outputDirectory": "apps/web/dist",
  "framework": null,
  "rewrites": [{ "source": "/((?!api/).*)", "destination": "/index.html" }]
}
